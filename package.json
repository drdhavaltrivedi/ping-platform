{
  "name": "ping-platform-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx expo export --platform web",
    "install-frontend": "cd frontend && npm install"
  }
}
